import { RenderGraphsPropsTypes } from "../types/propsTypes"
const RenderGraphs: React.FC<RenderGraphsPropsTypes> = ({ data }) => {
  return (
    <div>RenderGraphs</div>
  )
}

export default RenderGraphs